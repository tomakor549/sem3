#include "pch.h"
#include "Tplansza.h"
using namespace std;

void Tplansza::get() {
	/*
	talia.head->get();
	wcout << endl;
	wcout << endl;
	for (int i = 0; i < 7; i++) {
		stos[i].tail = stos[i].head;
		while (stos[i].tail != nullptr) {
			if (stos[i].tail->stan_karty() == 0) {
				wcout << "@\t";
			}
			else
			{
				stos[i].tail->get();
				wcout << " ";
			}
			stos[i].tail = stos[i].tail->next;
		}
		wcout << endl;
	}*/
}

Tplansza::Tplansza(Ttalia &talia)
{

}


Tplansza::~Tplansza()
{
}
